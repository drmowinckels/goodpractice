myFunc <- function(x ,y)
{
    if(x == NA & y > 0)return( x )
    z <- x+y
    # z <- z * 2
    w <- f (z)
    msg <- 'hello world'   
    x %>% sum() |> print()
    a_very_long_and_excessively_named_function_variable_name <- w
	return(z)
}


